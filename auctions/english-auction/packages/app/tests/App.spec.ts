import { test } from '@playwright/test';

// TODO figure out how to test with extension
test.describe('e2e', () => {
  test('Test auction expires', async () => {});

  test('Test auction is canceled', async () => {});
});
