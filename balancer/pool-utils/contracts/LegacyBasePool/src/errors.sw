library Errors;
/// All the errors related to legacy base pool

pub enum LegacyBasePoolErrors {
    MIN_TOKENS: (),
    MAX_TOKENS: (),
    MIN_SWAP_FEE_PERCENTAGE:(), 
    MAX_SWAP_FEE_PERCENTAGE:(),
    CALLER_NOT_VAULT: (),
    INVALID_POOL_ID: (),
    MINIMUM_BPT: (),
}